@model List<Note>
@{
    ViewBag.Title = "Index";
}

@*<header class="py-5 bg-light border-bottom mb-4">
        <div class="container">
            <div class="text-center my-5">
                <h1 class="fw-bolder">Welcome to Blog Home!</h1>
                <p class="lead mb-0">A Bootstrap 5 starter layout for your next blog homepage</p>
            </div>
        </div>
    </header>*@
<!-- Page content-->
<div class="container">
    <div class="row">
        <!-- Blog entries-->
        <div class="col-lg-8">
            <!-- Featured blog post-->
            <div class="card mb-4">
                <a href="#!"><img class="card-img-top" src="https://dummyimage.com/850x350/dee2e6/6c757d.jpg" alt="..." /></a>
                <div class="card-body">
                    <div class="small text-muted">@DateTime.Now</div>
                    <h2 class="card-title">Featured Post Title</h2>
                    <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Reiciendis aliquid atque, nulla? Quos cum ex quis soluta, a laboriosam. Dicta expedita corporis animi vero voluptate voluptatibus possimus, veniam magni quis!</p>
                    <a class="btn btn-primary" href="#!">Devamı →</a>
                </div>
            </div>
            <!-- Notlar-->
            <div class="row">
                @foreach (Note item in Model)
                {
                    <div class="col-lg-4" data-note-id="@item.ID">
                        <!-- Blog post-->
                        <div class="card mb-4">
                            <a href="#!">
                                <img class="card-img-top" src="https://dummyimage.com/700x350/dee2e6/6c757d.jpg" alt="@item.Title" />
                            </a>
                            <div class="card-body">
                                <div class="small text-muted pull-right">
                                    <span class="glyphicon glyphicon-user"></span>
                                    @if (item.NaciboUser.UserName == null)
                                    {
                                        @(item.NaciboUser.UserName = "cerenoztunc")

                                    }
                                    else
                                    {
                                        @item.NaciboUser.UserName
                                    }

                                </div><br />
                                <div class="small text-muted pull-right">
                                    <span class="glyphicon glyphicon-time"></span>
                                    @if (item.ModifiedDate != null)
                                    {
                                        @item.ModifiedDate
                                    }
                                    else
                                    {
                                        @item.CreatedDate
                                    }
                                </div>
                                <div>
                                    <h3 class="card-title">
                                        @if (item.Title.Length > 8)
                                        {
                                            <a href="#" data-toggle="modal" data-target="#modal_notedetail" data-note-id="@item.ID">
                                                @(item.Title.Substring(0,10) +"...")
                                            </a>

                                        }
                                        else
                                        {
                                            <a href="#" data-toggle="modal" data-target="#modal_notedetail" data-note-id="@item.ID">
                                                @item.Title
                                            </a>
                                        }

                                    </h3>
                                </div>
                                
                                
                                <p class="card-text">
                                    @if (item.Text != null && item.Text.Length < 60)
                                    {
                                        @item.Text
                                        <br />
                                        <a class="btn btn-primary btn-xs" href="#!" data-toggle="modal" data-target="#modal_notedetail" data-note-id="@item.ID">Tümü →</a>

                                    }
                                    else
                                    {
                                        @(item.Text.Substring(0,57) +"...")
                                        <br />
                                        <a class="btn btn-primary btn-xs" href="#!" data-toggle="modal" data-target="#modal_notedetail" data-note-id="@item.ID">Devamı →</a>

                                    }

                                    <hr />
                                </p>
                                <div class="ratings">
                                    <p class="pull-right">
                                        <button class="btn btn-default btn-sm" data-toggle="modal" data-target="#modal_comment" data-note-id="@item.ID" style="color:#e15959;">
                                            <span class="glyphicon glyphicon-comment"></span> Yorumlar
                                        </button>
                                    </p>
                                    <p>
                                        <button class="btn btn-default btn-sm" type="button" style="color: #e15959;" data-liked="false" data-note-id="@item.ID">
                                            <span class="glyphicon glyphicon-heart-empty like-heart"></span>&nbsp;                  <span class="like-count">@item.LikeCount</span>
                                        </button>

                                    </p>
                                </div>

                            </div>
                        </div>

                    </div>
                }

               
            </div>

            <!-- Pagination-->
            <nav aria-label="Pagination">
                <hr class="my-0" />
                <ul class="pagination justify-content-center my-4">
                    <li class="page-item disabled"><a class="page-link" href="#" tabindex="-1" aria-disabled="true">Newer</a></li>
                    <li class="page-item active" aria-current="page"><a class="page-link" href="#!">1</a></li>
                    <li class="page-item"><a class="page-link" href="#!">2</a></li>
                    <li class="page-item"><a class="page-link" href="#!">3</a></li>
                    <li class="page-item disabled"><a class="page-link" href="#!">...</a></li>
                    <li class="page-item"><a class="page-link" href="#!">15</a></li>
                    <li class="page-item"><a class="page-link" href="#!">Older</a></li>
                </ul>
            </nav>
        </div>

        <!-- Side widgets-->
        <div class="col-lg-4">
            <!-- Search widget-->
            <div class="card mb-4">
                <div class="card-header">Search</div>
                <div class="card-body">
                    <div class="input-group">
                        <input class="form-control" type="text" placeholder="Enter search term..." aria-label="Enter search term..." aria-describedby="button-search" />
                        <button class="btn btn-primary" id="button-search" type="button">Go!</button>
                    </div>
                </div>
            </div>
            <!-- Categories widget-->
            <div class="card mb-4">
                @Html.Partial("_PartialCategories")
            </div>
            <!-- Side widget-->
            <div class="card mb-4">
                <div class="card-header">Side Widget</div>
                <div class="card-body">You can put anything you want inside of these side widgets. They are easy to use, and feature the Bootstrap 5 card component!</div>
            </div>
        </div>
    </div>
</div>

@MyBootstrap.ModalPopup(id: "modal_comment", title: "Yorumlar")
@MyBootstrap.ModalPopup(id: "modal_notedetail", title: "Tüm Not")

<script src="~/Scripts/comment.js"></script>
<script src="~/Scripts/like.js"></script>
<script src="~/Scripts/NoteDetailPopup.js"></script>